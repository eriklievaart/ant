
filter
	target=clean

filter
	target=task-java-init, resolve, validate, task-checkstyle
	if=enable.java

filter
	target=osgi-dist
	if=enable.osgi & enable.dist

filter
	target=jar
	if=enable.jar

filter
	target=install
	if=enable.jar & enable.install

filter
	target=jar-deploy, run
	if=enable.jar & enable.application

filter
	target=war-deploy, task-war-static, task-webserver-start, task-webserver-stop
	if=enable.war

filter
	target=osgi-deploy
	if=enable.osgi & enable.application

filter
	target=osgi-install
	if=enable.osgi & enable.install

filter
	target=zip, task-zip-init
	if=enable.zip


